# Copy to .env (or set via OS env) for local dev.
# Cloud project values land here in Phase 4.

SUPABASE_URL=
SUPABASE_ANON_KEY=

# GitHub Releases (used by electron-updater + electron-builder publish)
GH_TOKEN=
