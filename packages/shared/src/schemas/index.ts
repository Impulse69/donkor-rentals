/**
 * Domain schemas (zod). Phase 1 fills these in: items, customers, contracts,
 * bookings, invoices, payments. Kept centralized so main + renderer share
 * one source of truth for validation.
 */

export {};
